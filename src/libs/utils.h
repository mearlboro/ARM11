#ifndef UTILS_H
#define UTILS_H

////////////////////////////////////////////////////////////////////////////////

#define HEAP_PRIM(type, value)\
	do\
	{\
		#type *pt = malloc(sizeof(#type));\
		*pt = value;\
		return pt;\
	} while(0)

int *heap_int(int);

////////////////////////////////////////////////////////////////////////////////

#endif
